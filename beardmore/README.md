# beardmore (legacy)

The **legacy Beardmore Glacier** grounding-zone study: 2013–2023 mixed
CS2+IS2 control eras on the wider hand-extended AOI, still on the legacy
shared-root layout (intentionally not ported to the harmonized batch
drivers). For current work on this geography use
[`../beardmore_shelf/`](../beardmore_shelf/) — the modern full-record
(2009→2024) study; the two share only the raw REMA strip pool under
`../data/REMA/strips/`.

Stage sequence: [`../PIPELINE.md`](../PIPELINE.md). Legacy status and
caveats: [`CLAUDE.md`](./CLAUDE.md). `config.py` holds the study context.
